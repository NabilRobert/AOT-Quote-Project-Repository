<%- include("header.ejs") %>
<body>
    <div class="quote-container">
        <% if (quote) { %>
            <h1>"<%= quote %>"</h1>
                <p>- <%= author %></p>
        <% } else { %>
            <h1>"I goon to Mikasa 24/7"</h1>
                <p>by Eren</p>
        <% } %>
    </div>
    <div class="button-container">
        <form action="/get-quote" method="POST">
            <button class="quote-button" type="submit">
                Get a quote!
            </button>
        </form>
    </div>
</body>
<%- include("footer.ejs") %>